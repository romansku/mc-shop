name: MarketSync
version: 1.0.0
main: org.game24.marketsync.MarketSync
description: Sync with site through DB
author: romansku
website: https://todo.org
api-version: '1.21.11'
bootstrapper: org.game24.marketsync.init.MarketSyncBootstrap

commands:
  marketsync:
    description: MarketSync admin commands
    usage: /marketsync reload
    permission: marketsync.reload
    permission-message: Недостаточно прав.

dependencies:
  server:
    AuthMe:
      load: BEFORE
    LuckPerms:
      load: BEFORE
    PlayerPoints:
      load: BEFORE
    PlaceholderAPI:
      load: BEFORE